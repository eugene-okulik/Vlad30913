
git --version

git status статус
git add добавить
git commit -m "added file hello.py"  подтвердить и сохранить

git status
git commit -am "update file hello.py"  подтвердить без add

git log история   выход из log состояния q
git checkout f7aa0374105241ebd9885b86fde2664d5e501424  возврат к предыдущему состоянию  ПО ID без комита
git checkout hello.txt  возврат к предыдущему состоянию без коммита

git reset Head hello.txt  отмена после git add
git revert Head  отмена  после git commit -m "a"  и выход из этого состояния Q

git checkout -b my создать ветку my
git branch  показ  где я
git checkout my переход на my
git checkout master  переход на master

git merge VideoKurs_2024  в нашу ветку вливаем файлы из ветки VideoKurs_2024

git push  export
git pull  import